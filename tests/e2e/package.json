{
  "name": "all-voice-e2e",
  "private": true,
  "description": "Playwright smoke test for the all-voice test UI (web/index.html).",
  "scripts": {
    "test": "playwright test",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
